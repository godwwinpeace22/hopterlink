VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
SUPABASE_PROJECT_REF=
VITE_APP_URL=http://localhost:5173

# Stripe wallet top-up (set as Supabase Edge Function secrets)
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# WALLET_TOPUP_SITE_URL=http://localhost:5173
# STRIPE_TOPUP_MIN_CENTS=100
# STRIPE_TOPUP_MAX_CENTS=500000

# Tip (workspace-safe when you have multiple Supabase projects):
# eval "$(./scripts/supabase-env.sh)"
# package.json already runs this before `npm run dev` and `npm run build`.
